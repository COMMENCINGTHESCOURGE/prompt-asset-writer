{
  "name": "prompt-asset-writer",
  "version": "0.1.0",
  "description": "Text-only: prompts, manifests, acceptance docs. No visuals, no audio, no network.",
  "main": "src/index.ts",
  "bin": {
    "prompt-asset-writer": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.19.41",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "files": [
    "src",
    "templates"
  ]
}
